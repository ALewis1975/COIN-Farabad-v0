name: SQF Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: SQF Lint
        uses: noblefry/sqf-lint-action@v1.0.0
        with:
          directory: .