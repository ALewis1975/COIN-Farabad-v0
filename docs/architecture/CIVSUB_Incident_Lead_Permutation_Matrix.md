# CIVSUB Incident/Lead Permutation Matrix (Civilian-Involving)

## Purpose

Capture the incident/lead permutations that involve civilians, then classify whether each permutation should be integrated into CIVSUB and how.

## Current contracts in code (source of truth)

- Runtime incident types currently generated by the incident selector are: `LOGISTICS`, `ESCORT`, `IED`, `RAID`, `DEFEND`, `QRF`, `PATROL`, `RECON`, `CIVIL`, `CHECKPOINT`. These come from the incident weighting switch in `fn_incidentCreate.sqf`. 
- Follow-on lead generation on incident close is handled in `fn_leadGenerateFromIncident.sqf` and is keyed by `(result, incidentType)` where `result ∈ {SUCCEEDED, FAILED}`.
- CIVSUB currently captures an incident-window baseline at incident acceptance (`activeIncidentCivsubDistrictId`, `activeIncidentCivsubStart`) and appends a CIVSUB annex during SITREP ingest.
- CIVSUB interaction events (ID checks, aid, detention, questioning) emit district deltas and counters.
- CIVSUB can include `lead_emit` inside CIVSUB bundles (ambient lead scheduler + crime DB hit), but there is currently no bridge that materializes those `lead_emit` envelopes into the core `leadPool` via `ARC_fnc_leadCreate`.

## Decision legend

- **YES (Now):** Integrate in CIVSUB immediately.
- **YES (Guarded):** Integrate, but with explicit guardrails/caps.
- **LATER:** Defer until dedicated-server validation and/or additional policy decisions.
- **NO:** Keep out of CIVSUB for now.

## Permutation matrix (incident close -> lead)

| Incident Type | Close Result | Existing Follow-on Lead Behavior | Civilian Surface | CIVSUB Integration Decision | How to integrate into CIVSUB |
|---|---|---|---|---|---|
| LOGISTICS | SUCCEEDED | May emit `ESCORT` follow-on only when sustainment need threshold is met. | Indirect civilian impact (service continuity, local confidence). | YES (Guarded) | Apply small district legitimacy/trust lift in affected district(s); keep lead probability driven by existing logistics need gate. |
| LOGISTICS | FAILED | Defaults to urgent `QRF` (or zone-specific failure handling). | Disruption/crowd anxiety around supply interruptions. | YES (Guarded) | Add fear spike + slight trust loss in incident district; avoid over-penalizing all districts. |
| ESCORT | SUCCEEDED | May emit `QRF` ambush-report style follow-on. | Route safety and civilian movement confidence. | YES (Guarded) | Add micro trust gain where route passes populated districts; cap to one district-weighted update per SITREP. |
| ESCORT | FAILED | Urgent escalation lead (zone-aware). | Civilian bystander risk and intimidation effects. | YES (Guarded) | Increment fear index and mild R pressure in impacted district. |
| IED | SUCCEEDED | Typical follow-on is `RAID` (bombmaker safehouse), with secure-zone avoidance. | Strong civilian security and confidence implications. | YES (Now) | Keep as primary CIVSUB-coupled incident; district deltas should consume evidence/disruption outcomes and witness-facing actions. |
| IED | FAILED | Urgent escalation lead, except when detonation queue already exists. | Civilian harm/fear potential is high. | YES (Now) | Record civilian casualty/trauma counters and apply FEAR + trust effects in annex delta. |
| RAID | SUCCEEDED | Often emits `DEFEND` retaliation-threat lead. | Civilian backlash/reprisal risk if raid handling is rough. | YES (Guarded) | Couple to detention fairness + collateral outcomes; positive if lawful/clean, negative if coercive or high collateral. |
| RAID | FAILED | Urgent escalation lead (zone-aware). | Heightened uncertainty and civilian intimidation. | YES (Now) | Add fear pressure and anti-cooperation penalty unless offset by humanitarian follow-up actions. |
| DEFEND | SUCCEEDED | No type-specific success branch in generator (falls through unless generalized behavior applies elsewhere). | Civilians perceive protective capacity. | LATER | Add once defend outcome telemetry includes civilian-protection signals (currently thin). |
| DEFEND | FAILED | Urgent escalation lead (zone-aware). | High civilian safety and displacement concerns. | YES (Now) | Negative district security sentiment delta and fear spike. |
| QRF | SUCCEEDED | No type-specific success branch in generator. | Civilians may observe rapid response quality. | LATER | Integrate after QRF objective telemetry includes collateral/safeguard fields. |
| QRF | FAILED | Urgent escalation lead (zone-aware). | Civilians experience visible loss of control. | YES (Now) | Apply trust/legitimacy loss + fear increase in primary district. |
| PATROL | SUCCEEDED | Can emit `RECON` pattern-of-life follow-on. | Routine civilian contact/rapport channel. | YES (Now) | Convert patrol civ-interaction quality (aid/respect/coercion) into W/G drift at SITREP annex time. |
| PATROL | FAILED | Urgent escalation lead (zone-aware). | Local confidence drop if patrol fails. | YES (Guarded) | Small W/G penalty; avoid punitive cascade unless repeated failures. |
| RECON | SUCCEEDED | Weighted branching across `RECON`, `CHECKPOINT`, `CIVIL`, `IED`, `RAID`. | Highest HUMINT/civilian-information surface. | YES (Now) | Promote as primary CIVSUB lead source; include district confidence + civ-contact quality in lead seed metadata. |
| RECON | FAILED | Urgent escalation lead (zone-aware). | Civilian cooperation can collapse after failed recon. | YES (Now) | Reduce cooperation score proxy and increase fear index in district. |
| CIVIL | SUCCEEDED | Usually emits `CHECKPOINT` or `RAID` depending on infiltration/corruption. | Direct civilian engagement by definition. | YES (Now) | Must always bind `activeIncidentCivsubDistrictId` and include full CIVSUB annex deltas/counters. |
| CIVIL | FAILED | Urgent escalation lead (zone-aware). | Direct reputational/legitimacy damage with population. | YES (Now) | Strong W/G penalties; trigger follow-up recommendation for aid + lawful detention pipeline. |
| CHECKPOINT | SUCCEEDED | Zone-dependent: Airbase/GreenZone/other branches to `RAID`/`RECON`/`CIVIL`/`IED`. | Civilian movement, screening fairness, and corruption perceptions. | YES (Now) | Integrate checkpoint fairness outcomes (hit quality, false-positive pressure, treatment quality) into district deltas. |
| CHECKPOINT | FAILED | Urgent escalation lead (zone-aware). | Civilians perceive loss of control and abuse risk. | YES (Now) | Fear increase + trust reduction; attach mitigation hint in annex. |

## CIVSUB-originated lead permutations (non-incident origin)

| CIVSUB Event | Current behavior | Integration status | Decision | Integration action |
|---|---|---|---|---|
| `CRIME_DB_HIT` from `civsubInteractCheckPapers` | Emits CIVSUB delta bundle with `lead_emit = { emit=true, lead_type="LEAD_DETAIN_SUSPECT", ... }`. | Partial (bundle only) | YES (Now) | Add server bridge: translate CIVSUB `lead_emit` to core `ARC_fnc_leadCreate` and persist resulting lead ID back into the bundle metadata. |
| `LEAD_AMBIENT` from `civsubSchedulerEmitAmbientLead` | Emits scheduler bundle containing randomized `lead_type` (`HUMINT/TIP/SUSPICIOUS_ACTIVITY`) and seed payload. | Partial (bundle only) | YES (Guarded) | Bridge to `leadPool` with cooldown + district caps; use conservative confidence threshold to avoid lead spam. |

## Required integration work (ordered)

1. **Lead bridge (highest value):**
   - Implement a server-side consumer for CIVSUB bundles where `lead_emit.emit == true`.
   - Normalize CIVSUB lead types to core incident types accepted by `ARC_fnc_leadCreate`/incident generator.
   - Write back created lead IDs for traceability.

2. **Incident district binding hardening:**
   - Ensure every accepted incident that touches civilians has `activeIncidentCivsubDistrictId` set deterministically.
   - Preserve a consistent start snapshot key shape (`activeIncidentCivsubStart*`) and use it in SITREP annex delta math.

3. **Annex -> policy feedback:**
   - Use annex deltas/counters as explicit gating hints for TOC closeout recommendations (not direct auto-close blockers).

4. **Guardrails:**
   - Keep server as single writer for CIVSUB district and shared state.
   - Cap CIVSUB-originated lead throughput (per district + global per hour).

## Acceptance criteria for this matrix

- Every runtime incident type is represented for both `SUCCEEDED` and `FAILED` outcomes.
- CIVSUB-originated lead permutations are explicitly represented.
- Each permutation has a deterministic integration decision (`YES (Now)`, `YES (Guarded)`, `LATER`, `NO`).
- Each `YES*` decision states exactly how integration should occur.

## Named CIVIL incidents: subsystem interaction review

The following rows cover the specific CIVIL scenarios requested, with static-analysis guidance on how civilians are present and how CIVSUB, THREAT, CORE, and Task/Lead layers should interact.

| CIVIL Incident | Civilian presence model | CIVSUB interaction model | THREAT interaction | CORE / Task-Lead interaction | Integration decision |
|---|---|---|---|---|---|
| Civil: Liaison in the Green Zone | Dense mixed population near governance/security cordon; high witness density and high fear sensitivity. | Bind incident district at accept; emphasize `SHOW_PAPERS`, `QUESTION`, and legitimacy-positive deltas unless coercion occurs. | Treat as insider/corruption pressure surface (low overt kinetic, moderate infiltration signal). | Keep `CIVIL` incident type; successful close can route to `CHECKPOINT`/`RAID` follow-ons per current generator. | YES (Now) |
| Civil: Community Engagement at Grand Mosque | Concentrated civilian gathering with community leaders and bystanders. | Strongly weight respectful-contact and aid effects (`AID_*`, `MED_AID_CIV`) and avoid hard-loss deltas unless abuse/collateral is explicit. | Add intimidation risk only when disruption/hostile interference is observed; avoid automatic RED escalation. | On success, prefer HUMINT-style follow-ons; on failure, attach fear/trust penalties in annex before closeout recommendation. | YES (Now) |
| Civil: VIP Meeting Security at Belle Fuelle Hotel | Civilian staff, attendees, local traffic; elevated crowd-control and false-positive risk. | Track checkpoint/search fairness and detention pipeline quality (initiation vs handoff) as first-class annex counters. | Moderate to high threat potential (insider or planned disruption), but keep attribution evidence-based. | Use existing `CIVIL`->follow-on behavior; if failure leads to escalation, annotate mitigation requirements in SITREP annex. | YES (Guarded) |
| Civil: Medical Outreach at Hospital | Vulnerable civilians, patients, and family clusters with high humanitarian sensitivity. | Prioritize `MED_AID_CIV` and aid events as primary trust/legitimacy drivers; collateral penalties should be amplified. | Threat should model opportunistic intimidation/sabotage, not default kinetic assumption. | Keep closeout coupled to CIVSUB annex outcome: positive medical engagement should materially offset background fear. | YES (Now) |
| Civil: Government Liaison at Palace | Civilians + government officials; politically visible interactions with legitimacy implications. | Weight governance legitimacy effects (`G`) more strongly in annex interpretation; preserve district-level counters for detention/casualty transparency. | Elevated insider-network risk; threat hooks should focus on infiltration and coercion indicators. | Success should maintain CIVIL pipeline; failures should generate strong legitimacy loss and follow-on stabilization recommendations. | YES (Now) |
| Civil: Embassy Liaison Meeting | Civilian workers/visitors with high diplomatic sensitivity and reputational stakes. | Use strict guardrails on coercive interactions; any detention/search action should require explicit compliance and leave auditable annex trail. | Threat layer should flag high-impact disruption attempts; avoid broad district punishment for isolated incidents. | Keep within `CIVIL` incident class; failed outcomes should bias toward containment/supportive follow-on, not indiscriminate kinetic branching. | YES (Guarded) |
| Civil: Crowd Control/Mediation | High-density civilian crowd, potentially fluid and stress-reactive. | Model crowd state via fear/trust deltas and aid/mediation actions; penalize coercive handling with hard-loss effects where justified. | Threat interaction is event-driven (agitators/intimidation) and should not assume every crowd is adversarial. | Core closeout should require annex-backed handling quality signals (casualties, aid, de-escalation evidence). | YES (Now) |

### Notes for these named incidents

- All seven scenarios should remain under the existing `CIVIL` incident-type umbrella in core incident generation unless/until a new public incident taxonomy is explicitly approved.
- CIVSUB should remain the authoritative source for civilian-facing interaction effects and counters; THREAT should consume those signals as context, not overwrite them.
- Server single-writer ownership remains unchanged: CORE writes incident lifecycle, CIVSUB writes civilian district/identity state, and Task/Lead owns lead/task lifecycle.

## Planning and task decomposition mode (implementation wiring plan)

This section translates the matrix into an execution backlog for wiring CIVSUB/THREAT/CORE/Task-Lead with minimal interface churn.

### Workstream 1 — CIVSUB lead bridge into core lead pool (P1)

**Goal:** materialize CIVSUB `lead_emit` envelopes into real leads (`leadPool`) while preserving server authority.

**Primary modules/files:**
- `functions/civsub/fn_civsubEmitDelta.sqf`
- `functions/civsub/fn_civsubDeltaApplyToDistrict.sqf`
- `functions/civsub/fn_civsubInteractCheckPapers.sqf`
- `functions/civsub/fn_civsubSchedulerEmitAmbientLead.sqf`
- `functions/core/fn_leadCreate.sqf` (consumer API)

**Tasks:**
1. Add a server-only CIVSUB bridge helper that inspects bundle `lead_emit` after validation.
2. Define a deterministic mapping from CIVSUB lead types (`LEAD_DETAIN_SUSPECT`, `HUMINT`, `TIP`, `SUSPICIOUS_ACTIVITY`) to core incident lead types.
3. Call `ARC_fnc_leadCreate` from the bridge and write created `lead_id` back into bundle metadata for SITREP/debug traceability.
4. Add caps: per-district hourly max + global hourly max for CIVSUB-originated leads.

**Exit criteria:**
- Crime DB hits and ambient leads can appear in `ARC_leadPoolPublic` through core lead pipeline.
- No client-side writes to shared state.

### Workstream 2 — CIVIL incident district binding hardening (P1)

**Goal:** guarantee every civilian-facing incident has a deterministic CIVSUB district context from accept through closeout.

**Primary modules/files:**
- `functions/core/fn_tocRequestAcceptIncident.sqf`
- `functions/core/fn_tocReceiveSitrep.sqf`
- `functions/civsub/fn_civsubSitrepAnnexBuild.sqf`

**Tasks:**
1. Normalize `activeIncidentCivsubDistrictId` assignment path (accept-time fallback by incident position).
2. Standardize start snapshot key usage (`activeIncidentCivsubStart` shape) and ensure annex builder reads the same key path.
3. For named CIVIL incidents, require annex generation to include district counters/deltas at SITREP submission.

**Exit criteria:**
- No accepted CIVIL incident reaches SITREP without district ID + annex baseline/end data.

### Workstream 3 — Named CIVIL scenario policy hooks (P2)

**Goal:** wire the seven named CIVIL scenarios into consistent, auditable effect policy without new public incident types.

**Primary modules/files:**
- `functions/civsub/fn_civsubDeltaValidate.sqf`
- `functions/civsub/fn_civsubDeltaApplyToDistrict.sqf`
- `functions/civsub/fn_civsubContactActionQuestion.sqf`
- `functions/civsub/fn_civsubInteractCheckPapers.sqf`
- `functions/core/fn_tocReceiveSitrep.sqf`

**Tasks:**
1. Encode scenario guidance as annex interpretation rules (e.g., medical outreach emphasizes `MED_AID_CIV`, crowd-control emphasizes fear/casualty handling quality).
2. Ensure coercive actions require compliance checks and generate auditable deltas for high-sensitivity scenarios (embassy/VIP).
3. Add mitigation hints in SITREP annex text for failure outcomes (aid follow-up, detention handoff quality, de-escalation actions).

**Exit criteria:**
- Each named CIVIL scenario has deterministic annex interpretation and closeout guidance path.

### Workstream 4 — THREAT coupling and guardrails (P2)

**Goal:** let THREAT consume CIVSUB context without taking ownership of CIVSUB state.

**Primary modules/files:**
- `functions/threat/*` (consumer side)
- `functions/civsub/fn_civsubSitrepAnnexBuild.sqf`
- `functions/core/fn_threadOnIncidentClosed.sqf` (for follow-on coupling checks)

**Tasks:**
1. Consume CIVSUB-derived signals (fear/trust/legitimacy deltas and relevant counters) as threat modifiers, not direct state mutations.
2. Keep attribution evidence-based: avoid broad district punishments for isolated incidents.
3. Verify follow-on generation remains owned by core lead/task pipeline.

**Exit criteria:**
- THREAT reads CIVSUB outputs and adjusts behavior probabilities; CIVSUB remains sole writer of district social state.

### Recommended implementation order

1. Workstream 1 (lead bridge)
2. Workstream 2 (district binding)
3. Workstream 3 (named CIVIL policy hooks)
4. Workstream 4 (THREAT coupling)

### Validation checklist (static + runtime-later)

- Static now:
  - Symbol/path checks for new bridge wiring and key usage.
  - `git --no-pager diff --check` on changed files.
- Runtime later (dedicated server required):
  - JIP correctness for lead bridge and annex snapshots.
  - Reconnect/respawn ownership behavior.
  - Throughput/cap behavior under dense civilian interactions.
